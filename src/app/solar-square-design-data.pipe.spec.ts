import { SolarSquareDesignDataPipe } from './solar-square-design-data.pipe';

describe('SolarSquareDesignDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SolarSquareDesignDataPipe();
    expect(pipe).toBeTruthy();
  });
});
